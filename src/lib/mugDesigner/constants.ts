export const MUG_WRAP = {
  width: 2000,
  height: 900,
} as const;

export const PRINT_ZONE = {
  x: 100,
  y: 50,
  width: 1800,
  height: 800,
} as const;

export const SAFE_ZONE_INSET = 60;
export const HANDLE_ZONE_WIDTH = 120;

export const MIN_IMAGE_SIDE = 120;
export const MAX_IMAGE_SCALE = 4;
export const MIN_INSIDE_RATIO = 0.2;

export const PREVIEW_MAX_SIZE_MB = 3;
