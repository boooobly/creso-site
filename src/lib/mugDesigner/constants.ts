export const MUG_WRAP = {
  width: 2000,
  height: 900,
} as const;

export const SAFE_ZONE_INSET = 40;
export const HANDLE_BAND_WIDTH = 140;

export const MIN_IMAGE_SIDE = 140;
export const MAX_IMAGE_SCALE = 5;
export const MIN_INSIDE_RATIO = 0.3;

export const PREVIEW_MAX_SIZE_MB = 3;
export const LAYOUT_MAX_SIZE_KB = 256;
